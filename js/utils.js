// return x, parameter x
function hi() {
    // alert("hi!!!!!")
    console.log("log!!!");
}

// return x, parameter o
function sum(x, y) {
    let result = x + y;
    console.log(result)
}

// return o, parameter o
function total(x, y) {
    return x + y;
}